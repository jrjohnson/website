<section class="post-list">
  <ul>
    {{#each @posts as |post|}}
      <li>
        [{{date-format post.attributes.date}}]
        {{#link-to "post" post.path}}
          {{post.attributes.title}}
        {{/link-to}}
      </li>
    {{/each}}
  </ul>
</section>
